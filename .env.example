# Database
DATABASE_URL="postgresql://user:password@localhost:5432/recipe_website"

# JWT Secret for Authentication
JWT_SECRET="your-secret-key-here-change-in-production"

# OpenAI API (for AI-powered recipe formatting)
OPENAI_API_KEY="your-openai-api-key"

# ==============================================================================
# Supabase Configuration - REQUIRED FOR FILE UPLOADS
# ==============================================================================
# This application uses Supabase Storage for file uploads (recipe images, etc.)
# You MUST configure these variables before running the application or seeding.
#
# HOW TO GET YOUR SUPABASE KEYS:
# 1. Go to https://app.supabase.com
# 2. Select your project (or create a new one if you don't have one)
# 3. Click on "Project Settings" (gear icon in sidebar)
# 4. Go to "API" section
# 5. Copy the following values:
#
# Project URL - Your Supabase project's API endpoint:
NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
#
# anon public key (copy from "Project API keys" > "anon public"):
# - This key is safe to use in client-side code
# - It respects Row Level Security (RLS) policies
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
#
# service_role key (copy from "Project API keys" > "service_role"):
# - ⚠️  WARNING: This is a SECRET key with admin privileges
# - NEVER expose this key in client-side code
# - NEVER commit this key to version control
# - Only use server-side (API routes, scripts)
SUPABASE_SERVICE_KEY="your-service-role-key"
#
# ==============================================================================
# IMPORTANT: You must also create a storage bucket in Supabase:
# 1. Go to Storage section in your Supabase dashboard
# 2. Create a new bucket named: recipe-builder
# 3. Enable "Public bucket" for public read access
# 4. See docs/SUPABASE_SETUP.md for detailed instructions
# ==============================================================================
